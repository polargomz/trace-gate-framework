schema_version: "0.1.0"
subframework_id: TRACE-AH
run_id: RUN.YYYYMMDD.HHMM.SHORT.NAME
trace_id: TRACE.YYYYMMDD.HHMM.SHORT.NAME
request_id: REQ.YYYYMMDD.HHMM.SHORT.NAME
created_at: 2026-01-01T09:00:00+09:00

context:
  access_intent_ref: ACCESS.YYYYMMDD.HHMM.SHORT.NAME
  required_context_pack_digest: null

limits:
  max_steps: 20
  max_tool_calls: 10
  deadline_seconds: 300
  max_context_bytes: 30720

tool_policy:
  allowed_tools:
    - repository.read
  authorization_refs: []
  read_only_parallel: true
  mutations_serial: true

verification:
  required: true
  verifier_ids:
    - deterministic.tests
  maximum_revisions: 3

termination:
  on_budget_exhaustion: failed
  on_permission_denial: blocked
  on_approval_required: awaiting_approval
